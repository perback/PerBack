<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowActionBar">false</item>
    </style>

    <style name="EditTextCustom" parent="android:Widget.EditText">
    </style>

    <style name="SmallRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:ratingBarStyle">@android:style/Widget.DeviceDefault.RatingBar.Small</item>
    </style>

</resources>
